language: php
php:
  - "5.4"

env:
  global:
    - CORE_BRANCH=stable7

branches:
  only:
    - travis_tests

before_install:
- sudo apt-get update
- sudo pip install requests

install:
  - bash ./before_install.sh $CORE_BRANCH
  - bash ./install_php_apache.sh

script:
  # execute unit tests
  - ./runtests.sh
